<?xml version="1.0" encoding="utf-8" ?>
<Page
    x:Class="BedrockBoot.Pages.TaskPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:dev="using:DevWinUI"
    xmlns:local="using:BedrockBoot.Pages"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:task="using:BedrockBoot.Controls"
    mc:Ignorable="d">

    <Grid Margin="40,48,40,0" Padding="0">
        <Grid
            Height="80"
            MaxWidth="1000"
            HorizontalAlignment="Stretch"
            VerticalAlignment="Top">
            <TextBlock
                x:Name="BreadcrumbBar"
                Margin="0,0,10,4"
                FontSize="30"
                Text="任务">
            </TextBlock>
        </Grid>
        <ScrollViewer
            Margin="0,72,0,0"
            HorizontalAlignment="Stretch">
            <Grid>
                <StackPanel x:Name="TaskContainer" HorizontalAlignment="Stretch" MaxWidth="1000"/>
            </Grid>
        </ScrollViewer>
    </Grid>
    <!--<Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <ScrollViewer HorizontalAlignment="Stretch"  Margin="20" Grid.Row="1">
            <StackPanel x:Name="TaskContainer"
                        HorizontalAlignment="Stretch">
            </StackPanel>
        </ScrollViewer>
    </Grid>-->
</Page>
