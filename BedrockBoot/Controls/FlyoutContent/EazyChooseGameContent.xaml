<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="BedrockBoot.Controls.FlyoutContent.EazyChooseGameContent"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BedrockBoot.Controls.FlyoutContent"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"></RowDefinition>
            <RowDefinition Height="*"></RowDefinition>
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal" Grid.Row="0">
            <ComboBox Width="280"
                      x:Name="FolderChooseBox"
                      SelectionChanged="FolderChooseBox_SelectionChanged"></ComboBox>
        </StackPanel>

        <ListView Height="280" 
                  Width="280"
                  Grid.Row="1"
                  Margin="0,20,0,0"
                  Background="Transparent"
                  SelectionChanged="GamesList_SelectionChanged"
                  x:Name="GamesList"></ListView>
    </Grid>
</Page>
