<windowFrame:OnePointWindow xmlns="https://github.com/avaloniaui"
                            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                            xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                            xmlns:windowFrame="clr-namespace:OnePointUI.Avalonia.Styling.Controls.OnePointControls.WindowFrame;assembly=OnePointUI.Avalonia"
                            xmlns:onePointControls="clr-namespace:OnePointUI.Avalonia.Styling.Controls.OnePointControls;assembly=OnePointUI.Avalonia"
                            mc:Ignorable="d" d:DesignWidth="800" d:DesignHeight="450"
                            x:Class="BedrockBoot.Views.Windows.ExceptionWindow"
                            IsMaxBtn="False"
                            IsMinBtn="False"
                            WindowStartupLocation="CenterOwner"
                            Width="800" Height="500"
                            MinWidth="800" MinHeight="500"
                            Title="BedrockBoot">

    <windowFrame:OnePointWindow.TitleBarContent>
        <onePointControls:LabelBox Text="{DynamicResource ExceptionWindow.TitleBar}"
                                   FontSize="14"
                                   Foreground="White"
                                   Background="DarkRed"
                                   Margin="0,0,5,0"
                                   VerticalAlignment="Center" />
    </windowFrame:OnePointWindow.TitleBarContent>

    <windowFrame:OnePointWindow.MainContent>
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <StackPanel Grid.Row="0" Orientation="Horizontal" Spacing="10" Margin="22,20,20,0">
                <onePointControls:FontIcon Glyph="&#xE814;"
                                           FontSize="38"
                                           Foreground="Orange"
                                           Margin="0,4,0,0"
                                           VerticalAlignment="Center" />
                <onePointControls:LineTextBlock FontSize="28"
                                                FontWeight="Bold"
                                                VerticalAlignment="Center"
                                                Text="{DynamicResource ExceptionWindow.FatalError.Header}" />
            </StackPanel>

            <TextBlock Grid.Row="1"
                       Text="{DynamicResource ExceptionWindow.FatalError.Message}"
                       Foreground="Gray"
                       Margin="20,5,20,0" />

            <ScrollViewer Grid.Row="2"
                          Margin="5,0,5,0">
                <StackPanel Margin="15">
                    <TextBlock TextWrapping="WrapWithOverflow"
                               FontFamily="Consolas"
                               Name="LogBox" />
                </StackPanel>
            </ScrollViewer>

            <StackPanel Grid.Row="3"
                        Margin="20"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        Spacing="10">
                <Button Name="SaveBtn"
                        Content="{DynamicResource ExceptionWindow.Button.Export}"
                        Padding="15,10"
                        Click="SaveBtnButton_OnClick"
                        Classes="NoBorder" />
                <Button Name="UploadBtn"
                        Content="上传日志"
                        Padding="15,10"
                        Click="UploadBtn_OnClick"
                        Classes="NoBorder" />
                <Button Name="CopyButton"
                        Content="{DynamicResource ExceptionWindow.Button.Copy}"
                        Padding="15,10"
                        Click="CopyButton_OnClick"
                        Classes="NoBorder" />
                <Button Name="CloseButton"
                        Content="{DynamicResource ExceptionWindow.Button.Return}"
                        Padding="15,10"
                        Click="CloseButton_OnClick"
                        Classes="NoBorder"
                        Background="{DynamicResource SystemFillColorCriticalBrush}"
                        Foreground="White" />
            </StackPanel>

            <StackPanel Grid.Row="3"
                        Margin="20"
                        Orientation="Horizontal"
                        HorizontalAlignment="Left"
                        Spacing="10">
                <HyperlinkButton Content="{DynamicResource ExceptionWindow.Link.FAQ}"
                                 NavigateUri="https://docs.roundstudio.top/docs/product/bb/commonQuestion" />
                <HyperlinkButton Content="{DynamicResource ExceptionWindow.Link.Issues}"
                                 NavigateUri="https://github.com/Round-Studio/BedrockBoot/issues" />
            </StackPanel>
        </Grid>
    </windowFrame:OnePointWindow.MainContent>
</windowFrame:OnePointWindow>